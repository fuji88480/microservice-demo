apiVersion: skaffold/v4beta3
kind: Config

manifests:
  rawYaml:
    - ./infra/k8s/*

build:
  local:
    push: false
  artifacts:
    - image: boss0613/auth-srv
      context: auth
      docker:
        dockerfile: Dockerfile
